{% extends 'admin_base.html.twig' %}

{% block title %}Gestion des Marques - Admin{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <h1 class="display-5 fw-bold">
            <i class="bi bi-award"></i> Gestion des <span class="text-gradient">Marques</span>
        </h1>
        <a href="{{ path('admin_brand_new') }}" class="btn btn-primary-glow">
            <i class="bi bi-plus-circle"></i> Nouvelle Marque
        </a>
    </div>

    {% for message in app.flashes('success') %}
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle"></i> {{ message }}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    {% endfor %}

    {% for message in app.flashes('error') %}
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle"></i> {{ message }}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    {% endfor %}

    <div class="glass-card p-4">
        <div class="table-responsive">
            <table class="table table-dark table-hover align-middle">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Slug</th>
                        <th>Description</th>
                        <th>Produits</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for brand in brands %}
                    <tr>
                        <td>{{ brand.id }}</td>
                        <td>
                            <div class="d-flex align-items-center gap-2">
                                {% if brand.logo %}
                                <img src="{{ brand.logo }}" alt="{{ brand.name }}"
                                     class="rounded" style="width: 40px; height: 40px; object-fit: contain; background: white; padding: 4px;">
                                {% else %}
                                <div class="rounded d-flex align-items-center justify-content-center"
                                     style="width: 40px; height: 40px; background: rgba(234, 179, 8, 0.2);">
                                    <i class="bi bi-award" style="color: #eab308;"></i>
                                </div>
                                {% endif %}
                                <strong>{{ brand.name }}</strong>
                            </div>
                        </td>
                        <td>
                            <code class="text-secondary small">{{ brand.slug }}</code>
                        </td>
                        <td>
                            {% if brand.description %}
                                <div style="max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                                    {{ brand.description }}
                                </div>
                            {% else %}
                                <span class="text-secondary fst-italic">Aucune description</span>
                            {% endif %}
                        </td>
                        <td>
                            <span class="badge" style="background: rgba(59, 130, 246, 0.2); color: #3b82f6;">
                                {{ brand.products|length }} produit(s)
                            </span>
                        </td>
                        <td class="text-end">
                            <div class="btn-group btn-group-sm">
                                <a href="{{ path('admin_brand_edit', {id: brand.id}) }}"
                                   class="btn btn-outline-primary">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form method="post" action="{{ path('admin_brand_delete', {id: brand.id}) }}"
                                      class="d-inline"
                                      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette marque ?');">
                                    <button type="submit" class="btn btn-outline-danger">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        {% if brands|length == 0 %}
        <div class="text-center py-5">
            <i class="bi bi-award display-1 text-secondary mb-3"></i>
            <p class="text-secondary">Aucune marque</p>
            <a href="{{ path('admin_brand_new') }}" class="btn btn-primary-glow mt-3">
                <i class="bi bi-plus-circle"></i> Créer une marque
            </a>
        </div>
        {% endif %}
    </div>

    <div class="mt-4">
        <a href="{{ path('admin_dashboard') }}" class="btn btn-glass">
            <i class="bi bi-arrow-left"></i> Retour au dashboard
        </a>
    </div>
</div>
{% endblock %}
