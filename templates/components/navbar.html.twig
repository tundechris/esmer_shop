<nav class="navbar-apple">
    <div class="navbar-content">
        <a href="{{ path('app_home') }}" class="navbar-brand">
            👟 D2E SHOP
        </a>

        <ul class="navbar-nav">
            <li><a href="{{ path('app_products') }}" class="nav-link">Produits</a></li>

            {% if app.user %}
                <li><a href="{{ path('app_wishlist') }}" class="nav-link">
                    <i class="bi bi-heart"></i>
                </a></li>
                <li><a href="{{ path('app_cart_index') }}" class="nav-link">
                    <i class="bi bi-bag"></i>
                </a></li>
                <li><a href="{{ path('app_orders') }}" class="nav-link">Commandes</a></li>
                <li><a href="{{ path('app_account') }}" class="nav-link">
                    {{ app.user.firstName }}
                </a></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('admin_dashboard') }}" class="nav-link">
                        Admin
                    </a></li>
                {% endif %}
            {% else %}
                <li><a href="{{ path('app_login') }}" class="nav-link">Connexion</a></li>
                <li><a href="{{ path('app_register') }}" class="btn-apple btn-apple-primary btn-apple-sm">Inscription</a></li>
            {% endif %}
        </ul>
    </div>
</nav>
